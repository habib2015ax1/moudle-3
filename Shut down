import os

def shutdown_computer():
    # For Windows: /s is shutdown, /t 1 is the delay in seconds
    if os.name == 'nt':
        os.system("shutdown /s /t 1")
    # For Linux/macOS: -h now initiates immediate shutdown
    else:
        os.system("shutdown -h now")

